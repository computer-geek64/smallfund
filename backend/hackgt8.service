[Unit]
Description=HackGT 8 Backend
Requires=NetworkManager.service
Requires=docker.service

[Service]
Type=simple
ExecStart=/usr/bin/docker --network host hackgt8
Restart=on-abort

[Install]
WantedBy=multi-user.target
